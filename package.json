{
  "name": "catalogue",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build --filter=!@catalogue-lab/example-app",
    "lint": "turbo lint",
    "test": "turbo test",
    "verify:publish": "node scripts/verify-publish.mjs",
    "changeset": "changeset",
    "changeset:auto": "node scripts/auto-changeset.mjs",
    "version": "changeset version",
    "publish": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "turbo": "^2.0.0",
    "typescript": "^5.5.4"
  }
}
